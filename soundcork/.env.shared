# Set this to the URL your speakers can reach this server on
# (e.g. http://192.168.1.50:8000). Don't use localhost or container names.
BASE_URL = "http://localhost:8000"

# Optional — defaults to ./data (relative to the launch directory).
# DATA_DIR = "/absolute/path/to/data"
